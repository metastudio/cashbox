$("#<%= dom_id(@transaction) %>").remove();
$("#<%= dom_id(@transaction, 'edit_row') %>").remove();

if ($('.transactions tbody tr').size() == 0) {
  $('.transactions_list').html("<%=j render partial: 'transactions/transactions_table', locals: { transactions: [] } %>");
}

$("#sidebar").replaceWith("<%= j render('home/sidebar') %>");
$("#total_balance").replaceWith("<%= j render('shared/layout/total_balance') %>");
