- css_class = money_bg(flow_amount)
td class=css_class
  = money_with_symbol(flow_amount)
- if flow.currency != def_curr
  td class=css_class
    = money_with_symbol(flow_amount.exchange_to(def_curr))
